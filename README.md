# MetricHit Landing

Готовый статический лендинг для загрузки в корень GitHub-репозитория и публикации через Vercel.

## Структура

- `index.html` — вся разметка, стили и JavaScript лендинга;
- `assets/` — логотипы, кейсы, favicon и Open Graph;
- `404.html` — страница ошибки;
- `robots.txt` и `sitemap.xml` — файлы для поисковых систем.

## Публикация

1. Загрузите всё содержимое этой папки в корень репозитория.
2. Подключите репозиторий к Vercel как статический проект без команды сборки.
3. После подключения постоянного домена замените `https://mtr-cz1o.vercel.app/` в `index.html`, `robots.txt` и `sitemap.xml`.

Основная регистрация ведёт на `https://mtrhit.ru/`, поддержка и выдача бонуса — на `https://t.me/Metric_Hit`.
