# Continuity перед переходом в новый Strategy-чат

- Дата: 31.08.2026.
- Источник: прямое поручение владельца зафиксировать важный проверенный контекст перед переходом в новый Strategy-чат.

## Подтверждённые глобальные навыки Codex

Установлены и проверены глобальные навыки, доступные будущим проектам без изменения их репозиториев, внешних сервисов или глобальной конфигурации:

- маркетинг: `marketing-brief`, `positioning`, `customer-research`, `content-strategy`, `seo-strategy`, `copywriting`, `cro-audit`, `analytics-tracking`, `email-sequence`;
- дизайн: `frontend-design`, `ui-ux`, `image`;
- разработка: `security-best-practices`, `playwright`, `code-review`, `architecture-review`, `debugging-and-error-recovery`, `gh-fix-ci`.

Навык выбирается по типу результата и scope задачи; он не является отдельным writer-исполнителем и не отменяет execution card, owner-gates или проверки. Установка не означает GitHub-авторизацию, развёртывание, внешние действия или изменение продукта.

## Исполнение и доступ владельца

Контролируемый pilot разрешает на одной задаче 2–3 заранее разделённые независимые read-only research/audit-ветки с обязательным synthesis Strategy. Эти ветки не изменяют repository, БД, память, конфигурацию или Git. Для mutation/code/data/config-задач неизменны один named writer, один commit и чистый Git.

Владелец работает с телефона: если platform approval действительно потребуется, Strategy запрашивает его в текущем чате. Личные имя и email не запрашиваются; для commit используется уже подтверждённая локальная идентичность репозитория `MetricHit Automation <metrichit@local.invalid>` без изменения глобального Git-конфига.

## Подпроект «Лендинг»

Подпроект «Лендинг» зарегистрирован как активный подпроект MetricHit с ID `2a95e640-23b0-44a6-96e5-f9f732cd41fa` и отдельной scoped memory. Локальные исходники: `work/landing/index.html` и `work/landing/404.html`. Публичный URL `https://go.mtrhit.ru/` отвечал HTTP 200 при read-only проверке. Публикация через GitHub возможна только после будущего предоставления доступа и отдельной команды; GitHub-авторизация, deploy, login и конфигурация не выполнялись.

## Состояние перехода

На момент сверки до continuity commit HEAD был `5b89e36d8d8e34ddb4da098aec0d285151e28b35`, рабочая копия чистая. Каноническим источником правил остаётся компактный `AGENTS.md` вместе с `documents/operating-context.md`, `documents/structured-memory.md` и `documents/roadmap.md`.
